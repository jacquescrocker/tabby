tell current window
    create tab with default profile
    tell the current session
        set name to "<%= title %>"
        write text "cd \"<%= basedir %>\" && clear"
        <%= commands %>
    end tell
end tell
